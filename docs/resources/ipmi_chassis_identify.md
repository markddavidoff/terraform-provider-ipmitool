---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "ipmi_chassis_identify Resource - ipmitool"
subcategory: ""
description: |-
  Blink the chassis identify LED. Operational convenience for spot-locating a host in a rack.
  This is an imperative resource: there's no IPMI "get identify duration" command, so Read is a no-op and drift is not detected. Use Terraform's replace_triggered_by if you want to re-trigger the LED on every apply.
---

# ipmi_chassis_identify (Resource)

Blink the chassis identify LED. Operational convenience for spot-locating a host in a rack.

This is an **imperative resource**: there's no IPMI "get identify duration" command, so Read is a no-op and drift is not detected. Use Terraform's `replace_triggered_by` if you want to re-trigger the LED on every apply.



<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `cipher_suite` (Number)
- `duration_seconds` (Number) How long the LED blinks (1..255). Ignored when `indefinite = true`. Default 15.
- `host` (String)
- `indefinite` (Boolean) If true, the LED stays on until explicitly cleared (or `destroy` if `off_on_destroy` is set).
- `interface` (String)
- `off_on_destroy` (Boolean) If true (default), `terraform destroy` clears the LED. Set false to leave the LED in whatever state the BMC last ran.
- `password` (String, Sensitive)
- `port` (Number)
- `username` (String)

### Read-Only

- `id` (String) The ID of this resource.
- `last_updated` (String)
