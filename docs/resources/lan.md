---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "ipmi_lan Resource - ipmi"
subcategory: ""
description: |-
  Manage BMC LAN configuration on one channel. Each set field is written; omitted fields are left alone on the BMC. Reads are per-selector and tolerate BMCs that don't implement every parameter (POC 3 found R210 II supports 20 of 24).
  Lockout warning: changing ip_address, ip_source, or vlan_id on channel 1 can break the provider's BMC connection. This resource requires explicit force_lockout_risk = true for those changes on channel 1.
---

# ipmi_lan (Resource)

Manage BMC LAN configuration on one channel. Each set field is written; omitted fields are left alone on the BMC. Reads are per-selector and tolerate BMCs that don't implement every parameter (POC 3 found R210 II supports 20 of 24).

**Lockout warning:** changing `ip_address`, `ip_source`, or `vlan_id` on channel 1 can break the provider's BMC connection. This resource requires explicit `force_lockout_risk = true` for those changes on channel 1.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `channel` (Number) IPMI channel number (1-11). The standard LAN channel is 1.

### Optional

- `backup_gateway` (String)
- `cipher_suite` (Number)
- `default_gateway` (String)
- `force_lockout_risk` (Boolean) Set true to override the channel-1 IP/VLAN lockout guard.
- `host` (String)
- `interface` (String)
- `ip_address` (String) BMC IPv4 address as `a.b.c.d`.
- `ip_source` (String) `static`, `dhcp`, `bios`, or `other`.
- `password` (String, Sensitive)
- `port` (Number)
- `primary_rmcp_port` (Number) IPMI/RMCP+ UDP port. Default 623.
- `subnet_mask` (String)
- `username` (String)
- `vlan_id` (Number) VLAN ID (1-4095). Set to 0 to disable VLAN tagging.
- `vlan_priority` (Number) 802.1p priority (0-7).

### Read-Only

- `id` (String) The ID of this resource.
- `last_updated` (String)
- `mac_address` (String) Read-only MAC address of the BMC interface.
- `vlan_enabled` (Boolean) True when the BMC reports VLAN tagging is active.
